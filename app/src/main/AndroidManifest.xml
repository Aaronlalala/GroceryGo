<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Include only if your app benefits from precise location access. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.GroceryGo"
        tools:targetApi="31">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyD2wom4N36GJPXlcPaANBIqK0fLZxMorxI" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
<!--        Replace action bar with tool bar in main activity-->
        <activity
            android:name=".layout.activity.ChattingActivity"
            android:exported="true">
            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
        <!-- Replace action bar with tool bar in main activity -->
        <activity
            android:name=".layout.activity.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.GroceryGo.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
        <activity android:name=".layout.activity.PostActivity" />

        <activity android:name=".layout.activity.ChatActivity" />


        <activity android:name=".layout.activity.PostDetailActivity" />

        <activity android:name=".layout.activity.RatingActivity" />
        <activity android:name=".layout.activity.HistoryActivity" />
    </application>

</manifest>